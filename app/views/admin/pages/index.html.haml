%h1 Listing pages
%table
  %thead
    %tr
      %th Name
      %th Title
      %th Description
      %th
      %th
      %th
  %tbody
    - @pages.each do |page|
      %tr
        %td= page.name
        %td= page.title
        %td= page.description
        %td= link_to 'Show', [:admin, page]
        %td= link_to 'Edit', edit_admin_page_path(page)
        %td= link_to 'Destroy', [:admin, page], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Page', new_admin_page_path
